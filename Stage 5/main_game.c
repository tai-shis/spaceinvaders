#include "main_game.h"

void spaceInvader(const Model *model, void *base) {
    render(&test, base, 1);

    int quit = 0;
    while (!quit) {
        /* Get input */
        /* Update model */
        /* Render */
    }
}